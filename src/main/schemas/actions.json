{
  "$schema": "http://json-schema.org/schema#",
  "required": [
    "action"
  ],
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "poweron",
        "poweroff",
        "reboot"
      ]
    }
  }
}